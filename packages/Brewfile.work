# Work-only Homebrew entries (e.g., corp-mandated apps).
# Populate this with casks/formulas you only want on a work Mac.
# Example:
# cask "microsoft-teams"

brew "awscli"
brew "go-task"
brew "redis"

cask "session-manager-plugin"
